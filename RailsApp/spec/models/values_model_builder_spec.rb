require 'spec_helper'

describe ValuesModelBuilder do

  subject(:values_model_builder) { ValuesModelBuilder.new(815) }
   
  describe "#build_or_get_model" do
    pending
  end
  
  describe "CountXXValue::values_array" do
    pending
    # Behavior to describe: if all their selects have the same data_type, 
    # it uses it, otherwise returns an array with strings
  end
  
end
